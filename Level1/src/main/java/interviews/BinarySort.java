package interviews;

import java.util.ArrayList;
import java.util.Scanner;

public class BinarySort {

	public static void main(String[] args) {
		BinarySort bs = new BinarySort();
		bs.binSort();
	}

	/**
	 * For a given list of integers, sort them by:
	 * * The number of 1's in their binary value
	 * * If two binary values has the same count of 1's then sort them as per integer
	 */
	private void binSort() {
		System.out.println(Integer.toBinaryString(10));
		ArrayList<Integer> l = new ArrayList<Integer>();
		Scanner s = new Scanner();
		
	}
}
